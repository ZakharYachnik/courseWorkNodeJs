<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/headerFooter.css">
    <link rel="stylesheet" href="/css/administration.css">
    <title>Administration</title>
    <%- include('blocks/admin') -%>
    <div class="container formDel">
        <form action="/administration"  method="post" autocomplete="off">
            <div class="container-fluid del_div">
                <span class="del">Удалить аккаунт пользователя по id</span> <input name ="del_id" type="number" min="0" class="num" placeholde="Введите id">
                <button type="submit" class="delete">Удалить</button>
            </div>
        </form>
        <% if ( errorInput !== '') { %>
          <div class="error">
            <p><%= errorInput %></p>
          </div>
        <% } %>

        <% if ( successfulDel !== '') { %>
          <div class="success">
            <p><%= successfulDel %></p>
          </div>
        <% } %>
    </div>

    <div class="container">
      <% if (numUsers > 0) { %>
        <table>
            <thead>
              <tr>
                <th>id</th> <th>Логин</th> <th>ФИО</th> <th>Телефон</th>
              </tr>
            </thead>
            <tbody>
                <% users.forEach((user) => { %>
                    <tr>
                        <td><%= user.id %></td>
                        <td><%= user.username %></td>
                        <td><%= user.full_name %></td> 
                        <td><%= user.phone_number %></td> 
                    </tr>
                <% }); %>
            </tbody>
          </table>
      <% } else {%>
        <h2>Зарегистрированных пользователей нет!!!</h2>
      <% }%>
    </div>
    <%- include('blocks/footer') -%>
</body>
</html>